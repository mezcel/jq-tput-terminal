#!/bin/bash

### install audio used in this app

function downloadOggFiles {

	currentDirPath=$(dirname $0)
	
	## wget if file is not locally installed
	wget -nc-i $currentDirPath/ogg-list.txt
}

ogg_audio_files
